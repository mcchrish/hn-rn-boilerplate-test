// import { fromJS } from 'immutable';
// import { makeSelectShowDomain } from '../selectors';

// const selector = makeSelectShowDomain();

describe('makeSelectShowDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
