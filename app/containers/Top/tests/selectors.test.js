// import { fromJS } from 'immutable';
// import { makeSelectTopDomain } from '../selectors';

// const selector = makeSelectTopDomain();

describe('makeSelectTopDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
